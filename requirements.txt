edge-tts>=6.1.0
numpy>=1.24.0
scipy>=1.10.0
soundfile>=0.12.0
librosa>=0.10.0
customtkinter>=5.2.0
flask>=3.0.0
flask-cors>=4.0.0
pywebview>=5.0.0

# NOTE: ffmpeg is NOT required for TTS. MP3→WAV conversion is handled in pure
# Python via soundfile (libsndfile >= 1.1.0 decodes MP3 natively) + librosa.
