edge-tts>=6.1.0
numpy>=1.24.0
scipy>=1.10.0
soundfile>=0.12.0
librosa>=0.10.0
customtkinter>=5.2.0
flask>=3.0.0
flask-cors>=4.0.0
pywebview>=5.0.0

# NOTE: ffmpeg.exe is required for MP3→WAV conversion.
# It is bundled with the .exe or must be installed on the system.
# Download: https://ffmpeg.org/download.html
